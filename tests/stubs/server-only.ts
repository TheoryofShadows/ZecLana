// Test stub for the `server-only` package. The real module throws if imported
// outside a React Server Component build, which would prevent unit-testing the
// pure helpers in lib/swap/oneclick.ts. Vitest aliases `server-only` to this.
export {}
